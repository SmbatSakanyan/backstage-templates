apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: example-all
  description: A collection of all Backstage example entities, except users, groups, and templates
spec:
  targets:
    - ./template.yaml
    - https://github.com/SmbatSakanyan/backstage-templates/blob/main/aws/template.yaml
    - https://github.com/SmbatSakanyan/backstage-templates/blob/main/aws/test/template.yaml
